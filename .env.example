# Not used yet — this app currently runs on mock data in lib/mockData.ts.
# When you connect Supabase (or Firebase), add your real keys here and
# copy this file to .env.local (which is gitignored).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
